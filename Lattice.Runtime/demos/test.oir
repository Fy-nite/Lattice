module Test version 1.0.0

class ModuleInfo {
    private field CompilerName: System.String
    private field CompilerVersion: Syste.String
    private field CompilerArgs: System.String
    private field DebugInfo: List<DebugInfo>
}

class Debuginfo {
    private field startline: System.int
    private field endline: System.int
    private field startColum: system.int
    private field endColum: System.int
    private field SourceFile: System.String
    private field instruction: System.String
}

class Program {
    method constructor() -> void {
        ret
    }

    static method Meowo() -> void {
        call Program.Meowt() -> void
    }

    static method Meowt() -> void {
        call Program.Meowv() -> void
    }

    static method Meowv() -> void {
        call Program.Meows() -> void
    }

    static method Meows() -> void {
        call IO.Println() -> void
    }

    static method Main(args: System.string[]) -> void {
        ldstr "$>"
        call IO.Print(System.string) -> void
        ldstr "meow"
        call IO.Readln() -> string
        cne
        if (stack) {
            ldstr "invalid command"
           call IO.Println(System.string) -> void
        }
        else
        {
            ldstr "invalid command"
            call Program.Meowo() -> void
        }
        ret
    }
}

