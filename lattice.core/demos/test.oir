module Test version 1.0.0
class Program
{
    method constructor() -> void {
        ret
    }
	static method Main() -> void {
        ldstr "$>"
        call  IO.Println(System.string) -> void
        ldstr "meow"
        call  IO.Readln() -> string
        cne
        if (stack)
        {
            ldstr "invalid command"
            call IO.Println(System.string) -> void
        }
        ret
    }
}